server:
  port: 8080
  
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
spring:
  boot:
    admin:
      client:
        instance:
          service-url: http://example-service:8080
        url: [http://sb-admin:9990]
  application:
    name: example-service